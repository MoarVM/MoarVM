name: Test compile in Alpine
on: [push, pull_request]

jobs:
  compile:
    runs-on: ubuntu-latest
    name: Compiles source
    container:
      image: JJ/raku-alpine-test@master
      
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Compile
        run: perl Configure.pl && make
